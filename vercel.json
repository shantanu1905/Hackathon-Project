{
    "builds": [{
        "src": "configurations/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "150mb", "runtime":"python3.7" }
    }

],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "configurations/wsgi.py"
        }
    ]
}